import SidebarMenu from "@/components/SidebarMenu/SidebarMenu";
import { Layout } from "@/styles/layout";

export default function PercentDiscount() {
  return (
    <Layout>
      <SidebarMenu />
      <h1>PercentDiscount</h1>
    </Layout>
  );
}
